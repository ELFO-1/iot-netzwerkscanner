# IoT Netzwerk Scanner v4 – Abhängigkeiten
# Installation: pip install -r requirements.txt

# Netzwerk-Scanning
python-nmap>=0.7.1

# Datenverarbeitung
pandas>=2.0
numpy>=1.24

# Machine Learning (Geräteklassifikation)
scikit-learn>=1.3

# Template-Engine (HTML-Berichte)
Jinja2>=3.1

# HTTP-/Basic-Auth-Tests, MAC-Vendor-Lookup
requests>=2.31

# Echte SSH-Login-/Brute-Force-Tests
paramiko>=3.0

# Excel-Export (.xlsx)
xlsxwriter>=3.0

# Web-Interface (optional – serve_reports)
Flask>=3.0

# PDF-Export (optional – export_to_pdf; benötigt System-Libs: pango, cairo, gdk-pixbuf)
weasyprint

# Hinweis: Die Metasploit-RPC-Anbindung liegt projektintern unter modules/msfrpc.py
# und ist KEIN pip-Paket. Metasploit selbst ist optional und wird separat installiert.

# Systemvoraussetzung (nicht via pip): nmap muss installiert und im PATH sein.
#   Arch/CachyOS:  sudo pacman -S nmap
#   Debian/Ubuntu: sudo apt install nmap
